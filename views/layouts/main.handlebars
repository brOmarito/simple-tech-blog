<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Fund My Project!</title>
        <link href="https://fonts.googleapis.com/css?family=Playfair+Display:700,900&amp;display=swap" rel="stylesheet">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
        <link rel="stylesheet" href="/css/style.css">
    </head>

    <body>
        <header class="blog-header py-3 header-sect">
            <div class="row flex-nowrap justify-content-between align-items-center">
                <div class="col-4 py-1">
                    {{#if loggedIn}}
                    <a class="link-secondary" href="/post/create">Create Post</a>
                    {{/if}}
                </div>
                <div class="col-4 text-center">
                    <a class="blog-header-logo text-dark header-text" href="/">Tech Blog</a>
                </div>
                <div class="col-4 d-flex justify-content-end align-items-center">
                    {{#if loggedIn}}
                        <a id="logout" class="btn btn-sm btn-outline-secondary" href="/">Logout</a>
                    {{else}}
                        <a class="btn btn-sm btn-outline-secondary" href="/login">Login / Register</a>
                    {{/if}}
                </div>
            </div>
        </header>
        <div class="container">
            <main class="content-section">
                {{{ body }}}
            </main>
        </div>
        <footer class="blog-footer">
            <p>Blog brought to you by <a href="https://getbootstrap.com/">brOmarito Works (?)</a>.</p>
            <p>
                <a href="#">Back to top</a>
            </p>
        </footer>
        {{#if loggedIn}}
            <script src="/js/logout.js"></script>
        {{/if}}
    </body>
</html>